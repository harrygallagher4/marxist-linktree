---
const predicate = (v: any) => v != null;
const truthValue = predicate(Astro.props['value']);
---
{Astro.slots.has('else') ?
  ((truthValue !== false) ? <><slot /></> : <><slot name="else" /></>)
  : (truthValue !== false) && <><slot /></>}

